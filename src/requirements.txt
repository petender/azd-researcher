flask>=2.0
python-dotenv>=0.21.0
aiohttp>=3.8.0
azure-core
# Azure SDKs
azure-identity>=1.13.0
azure-storage-blob>=12.14.0

# Azure AI SDKs (may be prerelease; if a prerelease is required, install with `pip install --pre azure-ai-projects`)
azure-ai-projects==1.1.0b3
azure-ai-agents==1.2.0b3

# Optional helpers
requests
gunicorn
